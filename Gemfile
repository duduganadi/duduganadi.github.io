source "https://rubygems.org"

# This Gemfile is for local development only. GitHub Pages builds your site
# server-side and ignores this file — but keeping it here lets you run
# `bundle exec jekyll serve` to preview locally.

gem "github-pages", group: :jekyll_plugins

# Performance-boosting plugins for some Windows setups; harmless elsewhere.
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem "wdm", "~> 0.1.1", platforms: [:mingw, :mswin, :x64_mingw]
